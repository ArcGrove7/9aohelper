# 給 Claude 的專案規矩

## 對話（人下的令，2026-09-06）

- **一律用繁體中文回話**。
- **時間一律用 UTC+8（台北時間）**——回話裡寫的時間、記在檔案裡給人看的時間、
  log 的時戳，全部都是 UTC+8，不要丟 UTC 或 ISO 的 Z 時間給人看。
  存進資料檔的機器欄位可以繼續用 ISO 8601，但顯示給人的那一份要換算過。

## 這個儲存庫是什麼

`9aohelper` 是圖鑑與攻略查表站（純靜態網站），另外附一組遊戲觀測／自動遊玩工具。
站本身的內容規矩寫在 `README.md` 的「內容規矩」一節，改站上的東西前先讀那裡。

## 自動遊玩工具（`tools/gao-play.js`）

- 帳號 token 放 `.gao-state/`，**已在 `.gitignore`，絕對不要進版控**。
- **每個帳號每小時請求上限 600**，程式壓在 500 留餘裕；額度存在磁碟上，
  換個行程重跑也接得回去。改額度改 `tools/gao/roster.js` 的 `hourlyCap`。
- 分工、路線、目標等級寫在 `tools/gao/roster.js`，主控迴圈不用動。
- 戰報的我方那一半（暱稱、英雄名、自家裝備）**不上站**，規矩見 `capture/README.md`。
