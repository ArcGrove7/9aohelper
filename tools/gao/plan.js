'use strict';
// 這一輪要跑的劇本。改這裡就好，主控迴圈不用動。

module.exports = {
  // 蒐證階段：全隊在哪裡原地打
  probe: { zoneId: 1, stage: 10 },

  // 挖礦組：人下的令（16:50）矮人工匠別挖礦了，專心鍛造，所以這裡清空。
  // 副作用：3096 沒有礦工了（429 西域霸天調去打架），泥土只能靠 ArcGrove 那邊
  // 的 Daedalus 挖了再轉過來。
  miners: {},

  // 一輪挖多久才收（分鐘）。人下的令：至少 30 分鐘一個階段，收太快沒有收穫。
  // 遊戲的最低時間是 3 分鐘，但收穫看消耗的體力——
  // 一到 3 分鐘就按完成，結果會是「消耗了 2 點體力，什麼也沒挖到」。
  miningMinutes: 30,

  // 鍛造組：英雄 id → 要打的東西
  //   randomName: 每次隨機取名（遊戲說名稱會影響成品好壞，總之不能空著）
  //   recipe 'soilWood': 泥土類的加成可以重複疊，但要有木頭類素材才會啟動
  //
  // 為什麼是盔甲不是武器（人下的令，2026-09-06）：
  //   **防具的數值永遠吃滿，武器吃不吃得滿要看鍛造者的該類武器熟練度。**
  //   大火慢炒（537）的熟練度：單手錘 1.10、短刀 1.00、盾牌 1.00，
  //   單手劍只有 0.80——先前打出來的「劣質的雷霆天降」攻 79 就是被這 0.80 折過的。
  //   要讓他打武器的話，選單手錘或短刀才不浪費素材。
  smiths: {
    // requireRecipe: false —— 人下的令（16:50）要工匠先鍛造，不要等素材。
    // 泥土已經用完，1 土 2 木湊不齊時退回一般配方（挑防禦值最高的素材）先打，
    // 有泥土進來就會自己恢復成加成配方。
    537: { type: 'armor', randomName: true, recipe: 'soilWood', requireRecipe: false, forgeSlot: 1 },
  },

  // 練功組（人組的三人隊，16:20）：
  //   816 黑色獠牙（lv10）、429 西域霸天（原「讓黑人再次偉大」，黑人戰士）、
  //   1041 鐵砂教頭（原「體育老師」）
  // 用 id 不用名字——這帳號的英雄會改名，id 才認得住。
  grinders: [816, 429, 1041],

  // 人下的令（16:45）：黑暗山洞的裝備與補品都不夠，先回城整備，再回大草原 7 樓練。
  // （黑暗山洞 1 樓一場 14 exp、是 7 樓的兩倍多，但三個人裡有一個空手，
  //   等工匠把盔甲補齊再回去。）
  route: [
    { zoneId: 1, zoneName: '大草原', stage: 7, untilLevel: 10 },
  ],

  // 人下的令：戰鬥人員的 HP 與體力都不准低於五成。
  // 低於這個比例就先吃補品，補不上來才休息。
  // 裝備耐久歸零就消失（實測一場狩獵吃掉約 1.4 點），所以壞了自動從庫存補上
  autoEquip: true,
  equipSlots: ['單手劍', '盔甲'],

  restBelow: 0.5,
  // 休息到 HP 與體力都回到這個比例才回去打。補一次就回去打會一直在門檻附近震盪，
  // 所以要休息到夠；但訂到 0.9 又太嚴——體力回得慢，實測整隊卡在休息、
  // 十五分鐘一場都沒打。0.75 距離五成的紅線還有緩衝，又打得動。
  restUntil: 0.75,
  // 補品補到這個比例就收手——庫存有限，剩下的交給休息
  healUntil: 0.65,
  // 要不要吃補品（補 HP／體力的）
  useItems: true,
  // 增益補品（戰鬥口糧那種）**只能留著打 BOSS**（人下的令）——練功時一律不准吃
  useBuffs: false,
  // 一輪休息多久（毫秒）——最低休息時間是 1 分鐘
  restMs: 5 * 60 * 1000,
  // 一次最多連休幾輪，一輪 restMs（五分鐘）。
  // 人下的令：16:26 先調成 1（休五分鐘就回去打），16:31 調回 3——
  // 一輪只補一百多點 HP，滿血一千多的隊員補不滿，下一場開打前又跌破五成、
  // 再休一輪，反而變成「打一場休五分鐘」比連休更黏。
  restRounds: 3,
};
